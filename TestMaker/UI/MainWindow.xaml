<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel x:Name="mainPanel">
        <Grid x:Name="buttonGrid" DockPanel.Dock="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button x:Name="prevButton" Content="&lt;-" Click="PrevButtonClick"></Button>
            <Button x:Name="nextButton" Content="->" Click="NextButtonClick" Grid.Column="1"></Button>
            <Button x:Name="finishButton" Content="Finish" Click="FinishButtonClick" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"></Button>
        </Grid>
    </DockPanel>
</Window>
